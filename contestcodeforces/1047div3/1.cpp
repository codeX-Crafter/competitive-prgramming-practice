#include <bits/stdc++.h>
using namespace std;
#define int long long

signed main()
{
    int t = 1;
    cin >> t;
    while (t--)
    {
        int k, x;
        cin >> k >> x;
        while (k--)
        {
            x = x * 2;
        }
        cout << x << endl;
    }
    return 0;
}